#pragma once

enum ClassType {
	Hunter,
	General,
	Neutral
};

enum CardType {
	Unit,
	Structure,
	Trap,
	Commander
};

enum TerrainType {
	Unpassable,
	Bonus,
	Normal
};

enum StatType {
	Health,
	Attack,
	Range,
	Movement
};